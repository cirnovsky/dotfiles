" muaa personal overrides (sourced by muaa-init.vim after the msmtprc-derived
" defaults, so these win). Not tracked in the vim-mail repo.
let g:mail_from = "cirnovsky <pup@guanyu.me>"
" Store moved under ~/Mail/all/ (mailboxes + .store nested one level down).
let g:mail_root = expand("~/Mail/all")

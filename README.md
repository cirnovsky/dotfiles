# dotfiles

My configuration for Vim, system fonts, fish, etc..

## Usage

Clone the respository to `$HOME` and run `ln.sh` to create symbolic links.

Run `PlugInstall` inside Vim afterwards. Selectively install CoC packages via `CocInstall` in reference to `coc-packages.json`.
